# Combining MF and MB agents in a Tolman detour task

My combined agents are in the `combined_agent2.py` file.

The agents are:

- `Baseline`
- `Adaptive`
- `Dissagreement`

I adapted `main.py` to run these agents in the experiment.

My plotting functions are in `plotter.py`.

`agents.py` was modified in accordance to the practical lab instructions.

Everything else was left as is.
